<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4" defer></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a href="/logout" class="px-3 py-2 bg-red-500 rounded-md">Logout</a>
        </div>
        
        
        <h2 class="text-3xl mt-5"><span class="font-light">Hello</span> , <%=user.name%> 🙌</h2>
        <h5 class="mb-5">You can create a new post.</h5>
        
        <form action="/createpost" method="post">

            <textarea class="bg-transparent bg-zinc-800 resize-none outline-none w-1/4 rounded-md b-2 p-3 block" placeholder="What's on your mind...." name="postcontent"></textarea>
            <button type="submit" class="px-3 py-2 bg-blue-500 rounded-md mt-4 w-40 text-sm">Create new post</button>

        </form>

        <div class="posts mt-10">
            <h3 class="text-zinc-400 mb-5">Your posts.</h3>

            <div class="post-container flex gap-2 flex-wrap">
                <%user.posts.forEach((post)=>{%>
                    <div class="post w-1/4 border-2 border-zinc-800 p-3 rounded-md bg-zinc-800">
                    <h4 class="text-green-400 mb-2"><%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %></small>
                    <div class="btns flex mt-1 gap-2">
                        <a href="/like/<%= post._id %>" class="px-1 py-1 text-blue-300 text-sm"><%= post.likes.indexOf(user._id) ===-1 ? "Like":"Unlike" %>
                        </a>
                        <a href="/edit/<%= post._id %>" class="px-1 py-1 text-zinc-300 text-sm">Edit</a>
                    </div>
                </div>
                <%})%>
            </div>


        </div>
            
        </div>
    </div>
</body>
</html>