<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4" defer></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        
        <h5 class="mb-5">You can create a new post.</h5>
        
        <form action="/update/<%= post._id %>" method="post">

            <textarea class="bg-transparent bg-zinc-800 resize-none outline-none w-1/4 rounded-md b-2 p-3 block" placeholder="What's on your mind...." name="postcontent"><%= post.content %></textarea>
            <button type="submit" class="px-3 py-2 bg-yellow-500 text-black rounded-md mt-4 w-40 text-sm">Edit your post</button>

        </form>

       
    </div>
</body>
</html>